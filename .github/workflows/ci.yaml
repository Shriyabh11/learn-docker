name: Docker CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Build image (runs tests)
        working-directory: calculator
        run: docker build -t calculator:test .
